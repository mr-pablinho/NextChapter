description = "Ingest raw CV data or profile text into the master bank"
prompt = """
Use the `cv-data-ingester` skill to parse the provided raw data and integrate it into my data/my_cv.yaml.

If the input is a file path (e.g., ends in .pdf, .docx, .txt), read the content of that file first. If it's raw text, parse it directly.

Here is the input:
{{args}}
"""
